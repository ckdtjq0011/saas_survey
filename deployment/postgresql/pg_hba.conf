# PostgreSQL Client Authentication Configuration
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections (Unix socket)
local   all             all                                     trust

# IPv4 local connections
host    all             all             127.0.0.1/32            scram-sha-256

# IPv6 local connections
host    all             all             ::1/128                 scram-sha-256

# Allow remote connections without SSL (adjust IP range as needed)
# Replace 0.0.0.0/0 with your specific IP range for better security
# Example: 10.10.20.0/24 for local network
# Example: YOUR_SERVER_IP/32 for specific IP
host    all             all             0.0.0.0/0               scram-sha-256

# Docker network connections
host    all             all             172.16.0.0/12           scram-sha-256
